using System;

namespace Ex03.ConsoleUI
{
    // $G$ DSN-999 (-10) All values whose range is discrete, finite and known should be represented as Enum.
    // $G$ DSN-999 (-10) If this Enum is a nested enum of this class it shuold be private. Otherwise it should be in a seperate file. (refgareding all Enums)

    public class Program
    {
        public static void Main()
        {
            UI.GarageMainMenu();
        }
    }
}
